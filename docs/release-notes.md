# Release notes

Sections below are used as the **GitHub Release** description for matching tags (see `.github/workflows/release.yml`). Add `## vX.Y.Z` **before** you push tag `vX.Y.Z`.

## v0.2.1

* In-app updates (GitHub Releases)
* macOS release builds on GitHub Actions (signed/notarized when Apple secrets are set)

## v0.2.0

* Quick capture hotkey (⌘ ⇧ K) and feedback entry wired in-app
* Fix empty stream when only pinned entries exist
* Edit shortcut listed in Settings
* Double-click prefers edit over open
* Settings shortcut tiles: clearer hover contrast

## v0.1.0

* Quick capture (⌘ ⇧ K)
* Feedback sharing
* Editing improvements
* UI polish
